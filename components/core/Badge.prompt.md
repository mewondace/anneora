Tiny uppercase pill for metadata — a release year, a genre tag, a "single" marker.

```jsx
<Badge>2025</Badge>
<Badge variant="glow">New single</Badge>
<Badge variant="soft">Ambient</Badge>
```

Variants: `outline` (default hairline), `soft` (faint fill), `glow` (haloed — for the freshest item only).
